import 'package:flutter/material.dart';

class HomePageYT extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Home')),
      body: Center(child: Text('Bienvenido a la página principal!')),
    );
  }
}

